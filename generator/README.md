# Generator

Reserved for the local resume build workflow.

Planned pieces:

- LaTeX build command, likely via `latexmk`.
- Optional Docker-based TeX Live build for reproducibility.
- Output paths for polished and ATS-safe PDFs.
- Text extraction checks after PDF generation.

No generator behavior has been added yet.
